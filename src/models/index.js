/**
 * Índice de modelos
 * Exporta todos los modelos para uso centralizado
 */

const Usuario = require('./Usuario');
const Producto = require('./Producto');

module.exports = {
  Usuario,
  Producto
};
